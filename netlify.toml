[build]
  base = "."
  command = "cd netlify/functions && npm install --omit=dev && cd ../.. && echo Build complete"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  CI = "true"
