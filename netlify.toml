[build]
  base = "."
  command = "echo Build complete"
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--prefer-offline --no-audit --fetch-timeout=60000 --fetch-retries=2"
  CI = "true"
